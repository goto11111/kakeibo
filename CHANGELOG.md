# preview.html 変更履歴

`preview.html`（給料サイクル ビュー・試作）のバージョン履歴です。
画面右上のバッジに現在のバージョンが出ます。タップすると同じ内容がその場で開きます。

本体 `index.html` は別管理で、このファイルの対象外です。

---

## v1.4.0 — 2026-09-17

- **【重要】「今日いくら使っていいか」を支払い方法ごとに表示する枠を最上部に新設。** 現金は今まさに回している給料から、カードは引き落とし日を賄う給料から出るため、使える枠が手段ごとに違う
- **これまで現金の枠を1ヶ月あとの給料プールで見ていた。** 給料日前は今日の現金支出が前の月の給料から出るため、正しいプールに直した
- カードごとに「今日買うと ◯/◯ 引き落とし → ◯月の給料から」を表示。締め日をまたぐカードは、その境目で引き落とし月が変わることも明記
- 判定は本体の `chargeSalaryMonth()` と同じ計算、金額は本体の各給料プールの残りそのもの
- **点検パネルに、本体とこの画面のカード設定の食い違いを警告する表示を追加**（例：三井住友カードが本体では26日払い、この画面では27日払い）

## v1.3.0 — 2026-09-17

- **「なぜその額しか使えないのか」を筋道で表示。** 今の給料の残り → 次の給料が足りない分をそこから補填 → だから今使っていいのはこれだけ、という流れにした
- **「📅 これからの予定」を追加。** いつ・いくらの引き落とし／給料／積立があるかを今日から日付順に並べ、「入ってくる合計」と「🔒 出ていくので取っておく合計」を出す
- **どの給料から出ていくかを2つのプールで表示。** カードの引き落としには実際に落ちる日付（例：9/27・10/10）と、カード別の内訳を併記
- プールの内訳は「元手 − 固定費 − 積立 − カードの引き落とし − もう使った分 ＝ この給料の残り」。食費予算は残りと相殺されるため式から省いた

## v1.2.0 — 2026-09-17

- **【不具合修正】「今月」の定義が本体とズレていた。** 本体は今日のカレンダー月（`currentMonth = now.getMonth()`）を既定にしているのに、こちらは今日を含む給料サイクルを見ていた。給料日（25日）より前は1ヶ月手前を指すため、**常に本体と1ヶ月ズレた数字を表示していた**。本体と同じカレンダー月に合わせた
- ヘッダを「◯年◯月の給料で回す分／◯/◯入金 ¥X → ◯/◯まで」に変更し、どの給料の話かを明示
- 給料日前で今日がその期間に入っていない場合は、今どの給料で生活中かを表示し、そちらへ移動できるリンクを追加

## v1.1.0 — 2026-09-17

- **主役の表示を、本体アプリの `monthTotalsFor()` が返す内訳をそのまま並べ替えたものに変更。** 式を作り直していないので、本体の「合計」と必ず一致する
- 給料プールごとに「**元手 − 固定費 − 食費予算 − 積立 − 先月カードの引き落とし ＝ 自由に使える枠 − もう使った分 ＝ 残り**」をタップで開ける形にした。取っておく分が本体の定義そのままで読める
- 先月カードの引き落としに、実際に落ちる日付を併記（例：8/27・9/10）
- **手元の現金を起点にした試算は折りたたみの中へ移動**（実験扱い）。本体の値とは別物であることを明示
- **「給料◯回」に臨時収入・返金が混ざって回数が実態と合っていなかったのを修正。** 給料と臨時・返金を別の行に分けた
- 繰越は本体が実残高として持っている値ではない旨を警告として明示

## v1.0.0 — 2026-09-16

- **上部の3パネル（本体の数字／手元の現金／サイクルの出入り）を1つに統合。** 「今の手元 ＋ 入る給料 − 取っておく分 ＝ いま自由に使っていい」の一本の流れにした
- **「取っておく分」を日付順の内訳で表示。** いつ・何に・いくら消えるかが1タップで見える（カード引き落とし／積立・返済／現金・口座振替／食費の残り予算）。来月のカード引き落としまで含めるので、「今の金を来月のカード分にも充てる」前提がそのまま反映される
- **「今の手元のうち、いくらが手をつけられないか」を明示。** これが実際に残しておくべき額
- 途中で残高が底を打つ場合は、期限時点の残りとは別に、その日を割らない範囲が使える上限である旨を注記
- 本体アプリの「合計」は参考値として1行に格納

## v0.9.0 — 2026-09-16

- **最上段の「いま使っていい額」を、本体アプリを実際に動かして得た数字そのものに変更。** 本体の「合計（今月の残り＋来月の使える分）」と必ず一致する
- その内訳を「**今の給料の残り**（現金で使える分）」と「**次の給料で使える分**（カードで切れる分）」の2段に分け、それぞれ給料日の日付つきで表示
- 手元の現金から組み立てる計算は下段へ移動（実験扱い）
- **積立・返済は本体が出している額をそのまま使う**よう変更。再実装の値とズレる場合は本体を優先
- **点検パネルに「本体の値との突き合わせ」を追加。** 給料・積立・固定費などを項目ごとに本体とこちらで並べ、食い違えば ⚠️ を出す。積立の内訳（目標ごとの今月の額）も一覧表示
- 手入力の手元現金が ¥0 のときは未入力扱いに（一度 0 を入れると全部 0 のまま戻らなくなっていた）
- 本体の起動完了後に、本体依存の表示を描き直すよう修正（起動待ちのまま「読み出せませんでした」と出ることがあった）

## v0.8.0 — 2026-09-16

- **手元の現金の入力を必須にするのをやめ、本体の「繰越」を起点に自動計算するよう戻した。** 給料も固定費も支出も積立も本体に入っているので、その月の繰越を出発点に日付順で足し引きすれば、何も入力しなくても今日の残高は出せる（v0.7.0 で入力必須にしたのは過剰だった）
- **起点が何なのかを画面に明記。** 「本体の◯月の繰越 ¥X から計算」／「手入力の ¥Y」を表示し、「自動に戻す」も追加
- 本体の繰越が ¥0 の月は、実額を入れると精度が上がる旨を表示
- **点検パネルに「繰越」の列を追加**し、起点の根拠を確認できるようにした

## v0.7.0 — 2026-09-16

- **手元の現金が未入力・0 のときは金額を出さず、入力を促す画面にした。** 0 のまま「使っていい額 ¥0」「残り ¥0」が並んで意味が分からなくなっていたため。本体は口座残高を持っていないので実残高の入力が要る、という理由も画面に明記
- **「本体アプリの表示をそのまま」は本流から外し、点検パネルの中へ畳んだ**（並べると混乱するため。突き合わせたいときだけ開く）
- **先読み範囲を「すでに使ったカードの引き落としが終わるまで」に限定。** 先の月に入力済みの予定まで追いかけて射程が何ヶ月も伸び、食費の確保が積み上がって過度に保守的になっていたのを修正
- **カードごとに締め日を表示**し、締め日をまたぐと引き落としが1ヶ月ずれる境目を明記（例：15日締めのカードで「今月の15日を過ぎているので 11/10 払い。15日までに買っていれば 10/10 でした」）
- 全カードが同額になる場合は、その理由（底になる日が共通）を表示
- 「このサイクルの出入り」の各項目に、いつ時点・どの期間の金額なのかを明記

## v0.6.0 — 2026-09-16

- **本体アプリのコードを隠しフレームで実際に起動し、本体が描いた数字をそのまま読み出す方式に変更。** これまでは本体の計算式をこちらに書き写していたため、写し損ねがあると数字がズレていた。式を写さなくなったのでズレようがない
- **「📱 本体アプリの表示をそのまま」を追加。** 食費／自由費の残り合計、現金（今月）／クレカの内訳、合計を本体と同じ並びで表示し、このページの「使っていい額」との差額も出す
- 本体を動かすときの localStorage は**メモリ上の偽物に差し替え**、`fetch` も塞ぐため、本物のデータにも Gist にも一切触れない（本体の `saveData()` が何度走っても実データは無傷）
- 本体と数字が違う理由（本体＝**予算の残り枠** ／ こちら＝**実際に手元にある金**）を注釈に明記
- データ元を切り替えたときにサンドボックスを再起動。二重起動時は後発を優先するガードを追加

## v0.5.0 — 2026-09-16

- **本体と同じ Gist から直接読み込めるように。** 起動時に自動でクラウドを読みに行くので、本体の最新の保存内容と必ず一致した状態で表示される。端末をまたいでも同じデータが見られる
- データ元を「☁️ Gist ／ 📱 端末 ／ 📄 JSON」で切り替え可能に。クラウドの最終保存時刻も表示
- Gist と端末で月数が違う場合に注意を表示（どちらが新しいかの手がかり）
- Gist が読めなかった場合は端末の保存データにフォールバックし、理由を表示
- **Gist への書き戻しは一切行わない（GET のみ）。** Token は本体が localStorage に持っているものを GitHub（または本体が設定した中継URL）へのリクエストにだけ使い、他のどこにも送らない

## v0.4.0 — 2026-09-16

- **【不具合修正】積立・返済が現金の流出として計上されていなかった。** 本体では毎月引かれている借金返済などが無視されていたため、「使っていい額」が月あたりその分だけ過大に出ていた（例：毎月4万円の返済なら、2ヶ月先まで見る場合に8万円ぶん甘い数字になっていた）
- **「🔍 取り込んだデータの点検」を追加**。月ごとの給料・固定費・支出・積立・食費枠を件数つきで表示し、給料0円や支出0件の月には警告を出す。返金として収入に足している額、積立の設定、カードの締め／引落日も一覧できる
- 手元の現金が未入力のときに「この金額はあてにならない」旨を大きく警告（それまでは本体の繰越を仮置きした数字が、そうと分からないまま出ていた）
- カレンダー・日別詳細・本体との対比にも積立・返済の行を追加

## v0.3.0 — 2026-09-16

- **「いま使っていい額」を最上部に新設**。手元の現金から、この先の引き落とし・現金支出・入ってくる給料を全部織り込み、「一番きつくなる日にいくら残るか」を出す方式に変更
- さらに**食費の残り予算を確保**したうえでの金額を「自由に使っていい額」として表示（⚙️でオフにできる）
- **手元の現金を直接入力**できるように（銀行残高＋財布の合計）。入力値を起点に全期間の残高を逆算する
- 先読み範囲を「次の給料日の次まで＋確定済みカード引き落としが終わるまで」に限定。支出未入力の先の月まで見て楽観的になるのを防ぐ
- 「いま買うなら、いくらまで」は上の新パネルへ統合
- **バージョン表示と更新履歴**を追加

## v0.2.0 — 2026-09-16

- カードと給料の対応を**カードごとに1ブロック**へ集約（同じカード名が複数ブロックに割れるのをやめた）
- **カレンダーの日付タップ**でその日の内訳（入金／カード引き落としの品目別／現金支出／その日のカード利用と引き落とし予定）を表示
- 説明文をすべて「ⓘ」の折りたたみへ格納し、画面を数字中心に簡素化。設定パネルを最下部へ移動
- **本体アプリの月次集計（`monthTotalsFor` 相当）を移植**し、同じ数字を並べて比較できるように。本体側の計算は画面上の上書き設定を無視し、常に本体の設定・給料日で行う
- 返金の参照キーの誤り（`ym` → `refundYM`）を修正。支出に紐づく返金予定も拾うように

## v0.1.0 — 2026-09-16

- 初版。給料日〜次の給料日の前日を1サイクルとして家計を見る試作ページを追加
- カード利用期間 FROM–TO → 引き落とし日 → 原資となる給料 の可視化
- 給料日起点のサイクルカレンダー、日ごとの手元残高、残高推移グラフ
- 既存データは localStorage から読み取り専用で取り込み（書き込みは一切なし）
